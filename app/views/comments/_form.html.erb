<h2>Add a comment:</h2>
<%= form_with model: [ commentable, commentable.comments.build ] do |f| %>
  <p>
    <%= f.label :body %>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
