<div class="grid gap-4 items-center px-6 h-12 bg-blue-500 grid-cols-[auto_1fr_auto_auto]">
  <div class="flex gap-2 justify-center items-center mr-4">
    <%= link_to root_path, { class: " text-3xl font-bold logo-font text-white" } do %>
      pawbook
      <%= image_tag "paw-3-svgrepo-com-white.svg", { class: 'logo-image', alt: 'A pawprint', size: "25x25" }%>
    <% end %>
  </div>
  <div>
    <div class="w-72 h-6 bg-white rounded-lg">search</div>
  </div>
  <div>notif.</div>
  <%= link_to root_path do %>
    <div class="avatar-circle avatar-circle-sm">
      <% if current_user.avatar.attached? %>
        <%= image_tag(current_user.avatar_as_thumbnail("small").url) %>
      <% else %>
        <%= current_user.username[0] %>
      <% end %>
    </div>
  <% end %>
</div>
