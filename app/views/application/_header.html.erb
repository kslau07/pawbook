<div class="px-6 grid grid-cols-[auto_1fr_auto_auto] items-center h-12 bg-blue-500">
  <div class="mr-4 flex justify-center items-center gap-2">
    <%= link_to "pawbook", root_path, { class: " text-3xl font-bold logo-font text-white" } %>
    <img class="logo-image" alt="A pawprint" src="/assets/paw-3-svgrepo-com-white-4e6202fbb52b4bc92f4bfd521bb93364eec3a8f0316ba19ce27c91c09061c06e.svg" width="25" height="25" />
  </div>
  <div>
    <div class="bg-white h-6 w-72 rounded-lg">search</div>
  </div>
  <div>notif.</div>
  <div class="bg-white outline outline-black">
    <% if current_user.avatar.attached? %>
      <%= link_to image_tag(current_user.avatar_as_thumbnail("small").url), dashboard_path if current_user.avatar.attached? %>
    <% else %>
      <div class="circle-sm text-8xl flex justify-center items-center">
        <%= current_user.username[0] %>
      </div>
    <% end %>
  </div>
</div>
