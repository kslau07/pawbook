<div class="px-6 grid grid-cols-[auto_1fr_auto_auto] gap-4 items-center h-12 bg-blue-500">
  <div class="mr-4 flex justify-center items-center gap-2">
    <%= link_to root_path, { class: " text-3xl font-bold logo-font text-white" } do %>
      pawbook
      <%= image_tag "paw-3-svgrepo-com-white.svg", { class: 'logo-image', alt: 'A pawprint', size: "25x25" }%>
    <% end %>
  </div>
  <div>
    <div class="bg-white h-6 w-72 rounded-lg">search</div>
  </div>
  <div>notif.</div>
  <div>
    <a href="<%= root_path %>" class="circle-sm text-lg flex justify-center items-center">
      <% if current_user.avatar.attached? %>
        <%= image_tag(current_user.avatar_as_thumbnail("small").url) %>
      <% else %>
        <%= current_user.username[0] %>
      <% end %>
    </a>
  </div>
</div>
